<!DOCTYPE html>
<html>

    <head>
    <title>{{ title }}</title>
    </head>

    <body>
        {{#if this.product}}
            <h1>{{ this.product.name}}</h1>
        {{/if}}

    </body>

</html>