<!DOCTYPE html>
<html>

    <head>
        <title>{{ title }}</title>
    </head>

    <body>

        <a href="/">Página principal</a>

        <div>
            {{#each products}}
            <a href="/products/product/"{{this.name}}>
                <h3>{{this.title}}</h3>
                <h3>{{this.description}}</h3>
                <h3>{{this.price}}</h3>
                <h3>{{this.image}}</h3>
            </a>
            {{/each}}
        </div>

    </body>

</html>